name: Pre-Commit

on:
  workflow_dispatch:
  pull_request:
  push:

jobs:
  pre-commit:
    strategy:
      fail-fast: false
      matrix:
        ros_distro: [humble, jazzy]
    uses: ./.github/workflows/reusable-pre-commit.yml
    with:
      ros_distro: ${{ matrix.ros_distro }}
